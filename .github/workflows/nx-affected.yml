name: Nx Affected CI
on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [12.x]
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}
      - run: git fetch origin master
      - name: Install Dependencies
        run: npm install
      - name: Run Affected Tests
        shell: bash
        run: |
          npm run build-all --base=remotes/origin/master
          npm run affected:e2e --base=remotes/origin/master
          npm run affected:libs --base=remotes/origin/master
          npm run affected:lint --base=remotes/origin/master
          npm run affected:test --base=remotes/origin/master
