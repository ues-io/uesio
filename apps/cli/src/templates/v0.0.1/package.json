{
	"name": "{{projectName}}",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"dev": "npm run watch-yaml & npm run watch-components",
		"watch-yaml": "nodemon --watch 'bundle/**/*' -e yaml --exec 'npx uesio deploy'",
		"watch-components": "nodemon --watch 'bundle/**/*' -i 'bundle/componentpacks/**/*'  -e tsx,ts,js --exec 'npx uesio pack && npx uesio deploy'",
		"seed": "./seed.sh"
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"@types/react": "^17.0.27",
		"@uesio/cli": "0.0.6",
		"@uesio/ui": "0.0.6",
		"react": "^17.0.2"
	},
	"dependencies": {
		"nodemon": "^2.0.15"
	}
}
