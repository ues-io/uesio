name: sitenav
definition:
  # Wires are how we pull in data
  wires:
  # Components are how we describe the layout of our view
  components:
    - io.list:
        components:
          - io.titlebar:
              uesio.variant: io.nav
              title: Site Admin
          - io.tile:
              uesio.variant: io.nav
              uesio.classes:
                selected:
                  - type: paramIsValue
                    param: selected
                    value: users
              signals:
                - signal: "route/NAVIGATE"
                  path: app/${["studio.app"]["uesio.id"]}/site/${studio.name}/users
              content:
                - io.text:
                    text: Users
              avatar:
                - io.text:
                    uesio.variant: io.icon
                    text: person
          - io.tile:
              uesio.variant: io.nav
              uesio.classes:
                selected:
                  - type: paramIsValue
                    param: selected
                    value: domains
              signals:
                - signal: "route/NAVIGATE"
                  path: app/${["studio.app"]["uesio.id"]}/site/${studio.name}/domains
              content:
                - io.text:
                    text: Domains
              avatar:
                - io.text:
                    uesio.variant: io.icon
                    text: verified_user
          - io.tile:
              uesio.variant: io.nav
              uesio.classes:
                selected:
                  - type: paramIsValue
                    param: selected
                    value: sitecollections
              signals:
                - signal: "route/NAVIGATE"
                  path: app/${["studio.app"]["uesio.id"]}/site/${studio.name}/collections
              content:
                - io.text:
                    text: Collections
              avatar:
                - io.text:
                    uesio.variant: io.icon
                    text: list
