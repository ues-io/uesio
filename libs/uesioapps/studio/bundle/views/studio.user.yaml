name: user
definition:
  # Wires are how we pull in data
  wires:
    apps:
      collection: uesio.apps
      fields:
        uesio.id:
        uesio.name:
        uesio.description:
        uesio.color:
      conditions:
        - field: uesio.name
          valueSource: PARAM
          param: appname
    sites:
      collection: uesio.sites
      fields:
        uesio.id:
        uesio.name:
        uesio.bundle:
        uesio.app:
          fields:
            uesio.id:
            uesio.name:
            uesio.color:
      conditions:
        - field: uesio.app
          valueSource: PARAM
          param: appname
        - field: uesio.name
          valueSource: PARAM
          param: sitename
    users:
      collection: uesio.users
      fields:
        uesio.id:
        uesio.firstname:
        uesio.lastname:
        uesio.federationId:
        uesio.federationType:
        uesio.profile:
        uesio.picture:
      conditions:
        - field: uesio.site
          valueSource: VALUE
          value: $Param{sitename}_$Param{appname}
        - field: uesio.federationId
          valueSource: VALUE
          value: $Param{userid}
  # Components are how we describe the layout of our view
  components:
    - io.grid:
        uesio.variant: studio.mainlayout
        items:
          - io.griditem:
              uesio.variant: studio.topbar
              components:
                - uesio.view:
                    view: studio.topbar
                    uesio.context:
                      wire: sites
          - io.griditem:
              uesio.variant: studio.leftbar
              components:
                - uesio.view:
                    view: studio.sitenav
                    uesio.context:
                      wire: sites
                    params:
                      selected: users
          - io.griditem:
              uesio.variant: studio.content
              components:
                - io.list:
                    id: userDeck
                    wire: users
                    mode: READ
                    components:
                      - io.titlebar:
                          uesio.variant: io.main
                          title: User - ${uesio.firstname} ${uesio.lastname}
                      - io.box:
                          uesio.variant: io.section
                          uesio.styles:
                            root:
                              padding: 40px
                          components:
                            - io.grid:
                                templateColumns:
                                  sm: 1fr
                                  md: 1fr 1fr
                                  lg: 1fr 3fr
                                columnGap: 40px
                                items:
                                  - io.griditem:
                                      components:
                                        - io.field:
                                            fieldId: uesio.picture
                                  - io.griditem:
                                      components:
                                        - io.field:
                                            fieldId: uesio.firstname
                                        - io.field:
                                            fieldId: uesio.lastname
                                        - io.field:
                                            fieldId: uesio.federationId
                                        - io.field:
                                            fieldId: uesio.federationType
                                        - io.field:
                                            fieldId: uesio.profile
  panels:
