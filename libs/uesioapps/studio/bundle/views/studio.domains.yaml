name: domains
definition:
  # Wires are how we pull in data
  wires:
    apps:
      collection: uesio.apps
      fields:
        uesio.id:
        uesio.name:
        uesio.description:
        uesio.color:
      conditions:
        - field: uesio.name
          valueSource: PARAM
          param: appname
    sites:
      collection: uesio.sites
      fields:
        uesio.id:
        uesio.name:
        uesio.bundle:
        uesio.app:
          fields:
            uesio.id:
            uesio.name:
            uesio.color:
      conditions:
        - field: uesio.appid
          valueSource: PARAM
          param: appname
        - field: uesio.name
          valueSource: PARAM
          param: sitename
    domains:
      collection: uesio.sitedomains
      fields:
        uesio.id:
        uesio.type:
        uesio.domain:
      conditions:
        - field: uesio.site
          valueSource: VALUE
          value: $Param{sitename}_$Param{appname}
    newdomain:
      collection: uesio.sitedomains
      fields:
        uesio.id:
        uesio.type:
        uesio.domain:
      type: CREATE
      defaults:
        - field: uesio.site
          valueSource: VALUE
          value: $Param{sitename}_$Param{appname}
        - field: uesio.type
          valueSource: VALUE
          value: subdomain
  # Components are how we describe the layout of our view
  components:
    - io.grid:
        uesio.variant: studio.mainlayout
        items:
          - io.griditem:
              uesio.variant: studio.topbar
              components:
                - uesio.view:
                    view: studio.topbar
                    uesio.context:
                      wire: sites
          - io.griditem:
              uesio.variant: studio.leftbar
              components:
                - uesio.view:
                    view: studio.sitenav
                    uesio.context:
                      wire: sites
                    params:
                      selected: domains
          - io.griditem:
              uesio.variant: studio.content
              components:
                - io.titlebar:
                    uesio.variant: io.main
                    title: Domains
                    subtitle: Add a domain to this site.
                    actions:
                      - io.group:
                          components:
                            - io.button:
                                text: "Add a Domain"
                                uesio.variant: io.secondary
                                signals:
                                  - signal: "panel/TOGGLE"
                                    panel: "newDomain"
                - io.box:
                    uesio.variant: io.section
                    components:
                      - io.deck:
                          id: domainslist
                          wire: domains
                          uesio.variant: io.section
                          components:
                            - io.tile:
                                uesio.variant: io.item
                                content:
                                  - io.text:
                                      text: ${uesio.type} ${uesio.domain}
                                avatar:
                                  - io.text:
                                      uesio.variant: io.icon
                                      color: "#ccc"
                                      text: verified_user
                                signals:
                                  - signal: "route/REDIRECT"
                                    path: "https://${uesio.domain}.$Site{domain}"
                                    newtab: true
  panels:
    - io.dialog:
        id: newDomain
        title: Add a Domain
        width: 400px
        height: 500px
        components:
          - io.list:
              id: newdomainlist
              wire: newdomain
              mode: EDIT
              components:
                - io.field:
                    fieldId: uesio.domain
        actions:
          - io.button:
              uesio.variant: "io.primary"
              text: "Save"
              signals:
                - signal: wire/SAVE
                  wires:
                    - newdomain
                - signal: "panel/TOGGLE"
                  panel: "newDomain"
                - signal: wire/EMPTY
                  wire: newdomain
                - signal: wire/CREATE_RECORD
                  wire: newdomain
                - signal: wire/LOAD
                  wires:
                    - domains
          - io.button:
              uesio.variant: "io.secondary"
              text: "Cancel"
              signals:
                - signal: "panel/TOGGLE"
                  panel: "newDomain"
                - signal: wire/CANCEL
                  wire: newdomain
