name: aws
type: uesio.aws
entries:
  region:
    type: configvalue
    value: crm.aws_region
  accessKeyId:
    type: secret
    value: crm.aws_access_key_id
  secretAccessKey:
    type: secret
    value: crm.aws_secret_access_key
