name: nav
component: io.button
label: Navigation
definition:
  "uesio.styles":
    root:
      color: "${theme.color.primary}"
      fontWeight: 500
      padding: 10px
      backgroundColor: inherit
      textTransform: uppercase
      "&:focus":
        outline: none
        color: ${theme.color.primary.darken}
