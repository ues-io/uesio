name: sites
definition:
  wires:
  components:
    - io.grid:
        uesio.variant: docs.mainlayout
        items:
          - io.griditem:
              uesio.variant: docs.topbar
              components:
                - uesio.view:
                    view: docs.topbar
          - io.griditem:
              uesio.variant: docs.leftbar
              components:
                - uesio.view:
                    view: docs.nav
                    params:
                      selected: sites
          - io.griditem:
              uesio.variant: docs.content
              components:
                - io.titlebar:
                    uesio.variant: io.main
                    title: Sites
                - io.box:
                    uesio.variant: io.section
                    components:
                      - io.text:
                          uesio.variant: io.section
                          text: Sites serve up the Bundle version you Packaged with your App using subdomains.
                      - io.titlebar:
                          uesio.variant: io.section
                          title: SITE ADMIN
                      - io.text:
                          uesio.variant: io.section
                          text: USERS - Administer the Users that will access the Site.
                      - io.text:
                          uesio.variant: io.section
                          text: DOMAINS - Create subdomains to use with the Site.
