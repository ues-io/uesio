name: home
definition:
  wires:
  components:
    - io.grid:
        uesio.variant: docs.mainlayout
        items:
          - io.griditem:
              uesio.variant: docs.topbar
              components:
                - uesio.view:
                    view: docs.topbar
          - io.griditem:
              uesio.variant: docs.leftbar
              components:
                - uesio.view:
                    view: docs.nav
                    params:
                      selected: home
          - io.griditem:
              uesio.variant: docs.content
              components:
                - io.titlebar:
                    uesio.variant: io.main
                    title: What is Uesio?
                - io.box:
                    uesio.variant: io.section
                    components:
                      - io.titlebar:
                          uesio.variant: io.secondary
                          title: UESIO – User Experience Studio
                - io.box:
                    uesio.variant: io.section
                    components:
                      - io.titlebar:
                          uesio.variant: io.tabbed
                          title: (u)ser (e)xperience (s)tud(io) - ues.io - (pronounced "yoo-zee-oh") is a web application builder where you work on configuration rather than code.
                - io.box:
                    uesio.variant: io.section
                    components:
                      - io.titlebar:
                          uesio.variant: io.tabbed
                          title: Think specifying the desired outcome, not the individual instructions to the computer.
                - io.box:
                    uesio.variant: io.section
                    components:
                      - io.titlebar:
                          uesio.variant: io.tabbed
                          title: Add shortcuts to your development cycles, share your coding talent and profit by listing in the Bundle Store.
