name: saml
type: STRUCT
label: SAML Credentials
subfields:
  - name: entity_id
    label: Entity ID (Config Value)
    type: METADATA
    metadata:
      type: CONFIGVALUE
  - name: metadata_xml_url
    label: Metadata XML URL (Config Value)
    type: METADATA
    metadata:
      type: CONFIGVALUE
  - name: metadata_xml
    label: Metadata XML (Config Value)
    type: METADATA
    metadata:
      type: CONFIGVALUE
  - name: certificate
    label: Certificate (Secret)
    type: METADATA
    required: true
    metadata:
      type: SECRET
  - name: private_key
    label: Private Key (Secret)
    type: METADATA
    required: true
    metadata:
      type: SECRET
