name: conditions
type: LIST
label: Conditions
subtype: STRUCT
subfields:
  - name: field
    type: METADATA
    label: User Reference Field
    metadata:
      type: FIELD
      grouping: ${uesio/studio.collection}
  # Currently - value is never used, so there's no point to exposing it here.
  # - name: value
  #   type: TEXT
  #   label: Value
