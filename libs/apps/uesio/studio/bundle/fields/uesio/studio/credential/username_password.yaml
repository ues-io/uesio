name: username_password
type: STRUCT
label: Username Password Credentials
subfields:
  - name: username
    label: Username (Secret)
    type: METADATA
    required: true
    metadata:
      type: SECRET
  - name: password
    label: Password (Secret)
    type: METADATA
    required: true
    metadata:
      type: SECRET
