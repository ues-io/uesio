name: aws_assume_role
type: STRUCT
label: AWS Assume Role
subfields:
  - name: region
    label: AWS Region (Config Value)
    type: METADATA
    required: true
    metadata:
      type: CONFIGVALUE
  - name: assume_role_arn
    label: ARN of AWS Role to assume (Secret)
    type: METADATA
    required: true
    metadata:
      type: SECRET
