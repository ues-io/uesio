name: aws_key
type: STRUCT
label: AWS Access Key Pair
subfields:
  - name: region
    label: AWS Region (Config Value)
    type: METADATA
    required: true
    metadata:
      type: CONFIGVALUE
  - name: access_key_id
    label: AWS Access Key Id (Secret)
    type: METADATA
    required: true
    metadata:
      type: SECRET
  - name: secret_access_key
    label: AWS Secret Access Key (Secret)
    type: METADATA
    required: true
    metadata:
      type: SECRET
  - name: session_token
    label: AWS Session Token
    type: METADATA
    metadata:
      type: SECRET
