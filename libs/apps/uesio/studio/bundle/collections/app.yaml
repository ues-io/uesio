name: app
idFormat: "${uesio/core.owner}/${uesio/studio.name}"
nameField: uesio/studio.name
access: "protected"
recordChallengeTokens:
  - token: "${uesio/core.id}"
    userAccessToken: "uesio/studio.teammember"
    access: readwrite
  - token: "${uesio/core.id}"
    userAccessToken: "uesio.installed"
    access: read
label: app
pluralLabel: apps
