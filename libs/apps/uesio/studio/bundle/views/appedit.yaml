name: appedit
definition:
  # Wires are how we pull in data
  wires:
  # Components are how we describe the layout of our view
  components:
    - io.list:
        id: editapplist
        mode: EDIT
        components:
          - io.field:
              fieldId: studio.name
          - io.field:
              fieldId: studio.description
          - io.field:
              fieldId: studio.color
          - studio.colorpicker:
              fieldId: studio.color
