name: login
public: true
definition:
  components:
    - uesio/core.view:
        view: uesio/appkit.login
        uesio.id: login
        params:
          expired: $Param{expired}
        slots:
          splash:
            - uesio/studio.splash:
  params:
    expired:
      type: CHECKBOX
      required: false
