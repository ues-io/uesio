name: declarative_default_variant
definition:
  # Wires connect to data in collections
  wires:
  # Components determine the layout and composition of your view
  components:
    - uesio/tests.declarative_default_variant:
        text: Some Value
