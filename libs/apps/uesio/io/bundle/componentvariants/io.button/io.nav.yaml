name: nav
component: io.button
label: Navigation
extends: io.default
definition:
  uesio.styles:
    root:
      color: "$Theme{color.primary}"
      fontWeight: 500
      padding: 10px
      textTransform: uppercase
      "&:focus":
        color: $Theme{color.primary.darken}
