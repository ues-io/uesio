import { FC } from "react"
import { Props } from "./filternumberdefinition"
import FilterNumber from "./filternumber"

const wrapper: FC<Props> = (props) => <FilterNumber {...props} />

export default wrapper
