{
	"name": "${name}",
	"version": "1.0.0",
	"description": "",
	"scripts": {
		"build": "uesio pack",
		"push": "uesio pack && uesio workspace deploy",
		"pull": "uesio workspace retrieve",
		"publish": "uesio bundle create"
	},
	"devDependencies": {
		"@types/react": "^18.2.0",
		"react": "^18.2.0"
	}
}
