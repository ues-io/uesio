# Wires are how we pull in data
wires:
# Components are how we describe the layout of our view
components:
  - uesio/io.viewheader:
      logo:
        - uesio/io.image:
            file: ${logo}
            height: ${logoheight}
      left: ${navcollections}
      right:
      avatar:
        - uesio/io.avatar:
            text: $User{initials}
            image: $User{picture}
