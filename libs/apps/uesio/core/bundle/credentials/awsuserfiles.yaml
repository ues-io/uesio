name: awsuserfiles
entries:
  region:
    type: configvalue
    value: uesio/core.aws_region
  accessKeyId:
    type: secret
    value: uesio/core.aws_access_key_id
  secretAccessKey:
    type: secret
    value: uesio/core.aws_secret_access_key
  sessionToken:
    type: secret
    value: uesio/core.aws_session_token
  bucket:
    type: configvalue
    value: uesio/core.userfiles_bucket_name
public: true
