name: view
title: View
description: A collection of wires, components and panels that represent a user interface.
category: LAYOUT
discoverable: true
properties:
  - name: view
    type: METADATA
    label: View
    metadataType: VIEW
  - name: uesio.id
    type: COMPONENT_ID
    label: Component Id
  - name: params
    type: PARAMS
    label: Params
sections:
  - type: HOME
    properties:
      - view
      - uesio.id
      - params
  - type: STYLES
  - type: DISPLAY
signals:
  SET_PARAM:
    label: "Set Param"
    properties:
      - name: param
        # TODO: Type "VIEW_PARAM" that's a dropdown of View parameters??
        type: TEXT
        label: Param Name
      - name: value
        type: TEXT
        label: Param Value
  SET_PARAMS:
    label: "Set Params"
    properties:
      # TODO: Fix builder.paramsfield so that we can specify a viewProperty providing the View Id,
      # vs just assuming there's a field "view" on the context record
      # - name: params
      #   type: PARAMS
      #   label: Params
