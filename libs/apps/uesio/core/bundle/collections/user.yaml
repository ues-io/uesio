name: user
uniqueKey:
  - uesio/core.username
public: true
label: user
pluralLabel: users
nameField: uesio/core.username
access: "protected_write"
recordChallengeTokens:
  - token: "${uesio/core.id}"
    userAccessToken: "uesio/core.orguser"
    access: readwrite
    conditions:
      - field: uesio/core.type
        value: ORG
