wires:
components:
  - uesio/io.viewlayout:
      uesio.variant: ${namespace}.page
      header:
        - uesio/core.view:
            view: ${namespace}.header
      content:
        - uesio/sitekit.section_hero:
            title: ${tagline}
            subtitle: ${tagline_sub}
        - uesio/io.box:
            components: ${mainTestimonialYaml}
        - uesio/sitekit.section:
            innerVariant: uesio/sitekit.section_inner_content
            uesio.styleTokens:
              inner:
                - grid-cols-3
                - gap-6
            content: ${testimonialsYaml}
      footer:
        - uesio/core.view:
            view: ${namespace}.footer
